# Codex Agents Notes

本文件用于给在此仓库内协作的 Codex/代理提供最小上下文，重点强调容易误改的业务约束。

## 项目概览

- 后端：`backend/app`
- 前端：`frontend/src`
- 数据库：SQLite，默认位于 `data/miyoushe.db`
- 主要场景：米游社扫码登录、游戏角色签到、日志查看、通知发送

## 高风险约束

### 1. 签到链路必须按 Starward 国服移动端实现保持完整

不要把签到修复理解成“只改一个参数”。当前链路是整套机制：

- `act_id`
- `x-rpc-signgame`
- Hyperion 风格 `User-Agent`
- `DS`
- `x-rpc-device_id`
- `x-rpc-device_fp`
- 查询后短延迟
- 角色间 finally 延迟

只删掉其中一个字段或等待逻辑，都可能造成星穹铁道重新失败，且表面现象通常只是“查询签到状态失败”。

### 2. 旧 SQLite 库兼容是必须项

本项目没有数据库迁移框架。任何新增表或重要结构时，都要考虑：

- 老部署直接拉代码重启后是否能跑
- 老库缺表时接口是否会直接 500
- 是否需要在服务层做按需补建或兼容处理

`system_settings` 就是典型案例：如果没有兼容逻辑，`/api/tasks/execute` 会因为缺表直接报 500。

### 3. 邮件配置分层不能混

- 系统 SMTP：管理员维护，决定系统是否能发信
- 用户通知邮箱：用户维护，决定邮件发给谁

这两个层级不能互相替代。排障时若混用，很容易误判成“邮箱已填但系统不发信”。

### 4. `/logs` 页优先保证可读性

日志页的核心目标不是“尽量塞更多列”，而是：

- 常规桌面宽度下不挤压
- 窄窗口下不破版
- 长错误消息能读到

如果要继续改表格，优先保留时间、账号、游戏、状态和详情的可读性。

## 推荐验证

后端：

```powershell
cd backend
.\.venv313\Scripts\python.exe -m unittest discover -s tests -v
.\.venv313\Scripts\python.exe -m compileall app
```

前端：

```powershell
cd frontend
npm run build
```

## 提交前检查

- 是否影响旧库兼容
- 是否破坏签到整套请求机制
- 是否误把系统 SMTP 和用户邮箱逻辑混用
- 是否补了必要中文注释与回归验证
